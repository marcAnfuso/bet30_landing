# Meta Pixel Configuration
# Obtené estos valores desde tu cuenta de Meta Business Suite
# https://business.facebook.com/

# ID del Pixel de Meta (público - se expone en el navegador)
NEXT_PUBLIC_META_PIXEL_ID=tu_pixel_id_aqui

# Token de acceso para Conversions API (privado - solo en servidor)
# Se genera en: Events Manager → Settings → Conversions API → Generate Access Token
META_CONVERSION_API_TOKEN=tu_token_aqui
